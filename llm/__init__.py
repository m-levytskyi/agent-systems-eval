"""LLM provider clients.

This package provides a small provider-agnostic interface so agents can swap
between model backends (e.g., local Ollama vs. hosted APIs) without changing
synthesis logic.
"""
